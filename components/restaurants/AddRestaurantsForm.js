import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

export default function AddRestaurantsForm( {toastRef, setLoading} ) {
    return (
        <View>
            <Text>AddRestaurantsForm...</Text>
        </View>
    )
}

const styles = StyleSheet.create({})
